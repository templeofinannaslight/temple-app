// Native target — passes straight through to the react-native-auth0 provider,
// which uses iOS Keychain / Android EncryptedSharedPreferences for persistence.
export { Auth0Provider } from "react-native-auth0"
